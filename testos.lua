function PrintTable(tbl)
    for k, v in pairs(tbl) do
      print(k, v)
    end
end
-- time = os.date()
-- print(time)
-- UI.ShowPrompt(false, "press A")
-- unix = os.time()
-- print(unix)
UI.ShowPrompt(false, "press A")
print(os.clock)
UI.ShowPrompt(false, "press A")
print(os.clock)
UI.ShowPrompt(false, "press A")